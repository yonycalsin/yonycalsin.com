import env from '~/utils/constants/env'

export const recommendationApiEndpoints = {
  /**
   * @todo use featured recommendations endpoint
   * @author yonycalsin
   */
  FEATURED_RECOMMENDATIONS: `${env.REST_API_URL}/recommendations`,
}
